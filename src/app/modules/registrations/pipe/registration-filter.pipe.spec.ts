import { RegistrationFilterPipe } from './registration-filter.pipe';

describe('RegistrationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RegistrationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
